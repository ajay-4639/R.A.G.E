# RAG OS

A fully customizable RAG Operating System - pipeline compiler and runtime.

## Installation

```bash
pip install -e ".[dev]"
```

## Usage

```python
from rag_os.core import Step, StepContext, StepResult, StepType
```
